<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "=//mybatis.org//DTO Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kopo.poly.persistance.mapper.ICalendarMapper">

<!--    <select id="getCalendarList" parameterType="CalendarDto" resultType="CalendarDto">-->
<!--        SELECT * FROM CALENDAR_INFO-->
<!--        WHERE userId = #{userId}-->
<!--    </select>-->


    <select id="getpaylist" parameterType="PayDTO" resultType="PayDTO">
        SELECT * FROM PAY
        WHERE MARKET_SEQ=#{market_seq}
    </select>


    <select id="getpuList" parameterType="PayDTO" resultType="PayDTO">
        SELECT * FROM PAY
        WHERE USER_SEQ=#{user_seq}
    </select>





















</mapper>